<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.gitfy.gitfyapi.dao.IUserDao">

    <insert id="addUser" parameterType="userVO">
        insert into users(uid, telegram, isadmin)
        values (#{uid}, #{telegram}, #{isAdmin})
    </insert>


    <delete id="deleteUser">
        delete
        from users
        where uid = #{uid}
          and telegram = #{telegram}
    </delete>

    <select id="findUser" parameterType="string" resultType="userVO">
        select uid, telegram, isadmin
        from users
        where telegram = #{telegram}
    </select>

    <insert id="followRepo">
        insert into follows(uid, platform, owner, name)
        values (#{uid}, #{repo.platform}, #{repo.owner}, #{repo.name})
    </insert>

    <delete id="unFollowRepo">
        delete
        from follows
        where uid = #{uid}
          and platform = #{repo.platform}
          and owner = #{repo.owner}
          and name = #{repo.name}
    </delete>
</mapper>