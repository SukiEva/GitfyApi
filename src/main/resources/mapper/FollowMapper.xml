<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gitfy.gitfyapi.mapper.FollowMapper">
    <select id="getFollowByUid" parameterType="string" resultType="repo">
        select platform, owner, repo
        from follows
        where uid = #{uid}
    </select>

    <select id="ifRepoFollowed" resultType="int">
        select Count(*)
        from follows
        where uid = #{uid}
          and platform = #{platform}
          and owner = #{owner}
          and repo = #{repo}
    </select>
</mapper>